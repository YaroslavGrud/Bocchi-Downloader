# Политика безопасности Bocchi Downloader

Спасибо, что помогаете сделать Bocchi Downloader безопаснее! Мы серьезно относимся к любым уязвимостям и стремимся оперативно их исправлять.

> [!CAUTION]
> **Ваш токен Яндекс.Музыки может быть виден на сервере** \
> **Передавая его, вы действуете на свой риск. Это предупреждение указано в пункте 6.2 [лицензии](https://github.com/YaroslavGrud/Bocchi-Downloader/blob/Yaroslav_grud/LICENSE#L172).**
>> **6.2. Пользователь самостоятельно обеспечивает безопасность своих токенов
и несёт полную ответственность за последствия их использования в ПО.**
> ### В чём проблема?
> - Чтобы скачать музыку, бот запускает внешнюю программу-загрузчик и передаёт ей ваш токен.  
> - В Linux любой, у кого есть доступ к серверу, может подсмотреть аргументы командной строки запущенных программ.  
> - Таким образом, токен может стать видимым для других пользователей на том же сервере.
>
> ### Почему это нельзя исправить простым шифрованием?
> 1. **Сама программа-загрузчик не умеет работать с зашифрованными токенами** – ей нужен именно оригинальный токен.  
> 2. **Передача токена через переменные окружения не помогает** – их тоже можно прочитать.  
> 3. **Даже если мы храним токены в базе данных зашифрованными**, перед запуском загрузчика их всё равно приходится расшифровывать и передавать открыто.  
> 4. **Полная изоляция требует отдельной прослойки в среде, изолированной от основного хостинга** – это сложно и выходит за рамки проекта.
>
> ### Что вы можете сделать, чтобы обезопасить себя?
> - Запускайте бота **на отдельном сервере**, к которому ни у кого нет доступа.  
> - Если вы единственный человек, использующий бота, и сервер только ваш – риска почти нет.

> [!Warning]
> Веб-дашборды, упомянутые в [README](https://github.com/YaroslavGrud/Bocchi-Downloader/blob/Yaroslav_grud/README.md), работают исключительно через протокол HTTP.
>
> ### Переход на HTTPS невозможен, так как:
>
> - У сервера нет собственного доменного имени. `Let's Encrypt` выдает сертификаты только для доменов, а не для IP-адресов.
> - Применение самоподписанного сертификата приводит к тому, что в браузере возникает фатальная ошибка безопасности, связанная с отсутствием доверия к сертификатам.
> - Чтобы получить надёжный SSL-сертификат, нужно приобрести домен и настроить DNS, однако в настоящее время эта возможность недоступна.
> 
> Советую применять дашборды исключительно в защищённой внутренней сети или временно, пока не будет куплен домен и не будет осуществлён переход на HTTPS.

## 📦 Область действия политики

Данная политика безопасности распространяется на **все файлы и зависимости**, находящиеся в этом репозитории. Это включает, но не ограничивается:

*   Исходный код Python (`.py`).
*   Файлы конфигурации (`.json`, `.yaml`, `.env.example`).
*   Списки зависимостей (`requirements.txt`, `pyproject.toml`, `Pipfile`).
*   Скрипты автоматизации и Docker-файлы.
*   Любые бинарные файлы или ресурсы, входящие в состав релизов.

Если уязвимость обнаружена в **сторонней зависимости** (библиотеке, указанной в `requirements.txt`), мы будем признательны за сообщение о ней. Даже если мы не можем исправить саму библиотеку, мы можем обновить версию зависимости или применить обходное решение (workaround) в коде проекта.

## 📋 Поддерживаемые версии

Обновления безопасности выпускаются только для следующих версий проекта:

| Версия                           | Поддержка обновлениями |
|:---------------------------------|:----------------------:|
| `main` (ветка разработки)        |          ✅ Да          |
| Последний релиз (Latest Release) |          ✅ Да          |
| Устаревшие версии (< 1.0.0)      |         ❌ Нет          |

> **Примечание:** Если вы используете старую версию, мы настоятельно рекомендуем обновиться до последней доступной, так как исправления для старых версий не предоставляются.

## 🛡️ Как сообщить об уязвимости

**⚠️ Пожалуйста, НЕ создавайте публичные Issue для обсуждения уязвимостей.**  
Публичное раскрытие может поставить под угрозу пользователей, которые еще не установили исправление.

### Инструкция по отправке отчета

Для сообщения об уязвимости используйте **исключительно** встроенный механизм приватного информирования GitHub:

1.  Перейдите на вкладку **Security** в верхней части этого репозитория.
2.  Выберите раздел **Advisories** (в левом меню).
3.  Нажмите зеленую кнопку **"Report a vulnerability"**.
4.  Заполните форму, подробно описав проблему и шаги для её воспроизведения.

Этот метод гарантирует, что сообщение увидят только мейнтейнеры проекта до тех пор, пока исправление не будет готово к публичному релизу.

## ⏳ Чего ожидать после отправки отчета

1.  **Подтверждение:** Мы постараемся ответить через GitHub Advisory в течение **48 часов**, подтвердив получение вашего сообщения.
2.  **Анализ:** Мы проведем оценку влияния уязвимости на проект, включая проверку зависимостей, указанных в `requirements.txt` и `pyproject.toml`.
3.  **Исправление:** Мы разработаем патч и выпустим новую версию. Если уязвимость критическая, мы можем временно откатить опасную функциональность или заблокировать проблемную зависимость.
4.  **Публичное раскрытие:** После выпуска исправления мы опубликуем Security Advisory с благодарностью вам (если вы захотите быть указанным) и техническими деталями проблемы.

## 🔒 Заявление об отказе от ответственности

Данный инструмент предназначен исключительно для легального скачивания контента из открытых источников. Пожалуйста, убедитесь, что ваше использование проекта соответствует законодательству вашей страны и условиям использования соответствующих платформ. Сообщения об уязвимостях, связанных с нецелевым использованием инструмента, не рассматриваются.

Благодарим вас за помощь в улучшении безопасности проекта Bocchi Downloader!
